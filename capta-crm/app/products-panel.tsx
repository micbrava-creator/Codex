"use client";
import { FormEvent, useCallback, useEffect, useState } from "react";

type Product = { id: string; name: string; category: string; description: string; priceCents: number; pipelineId: string | null; pipelineName: string; color: string; active: boolean };
type Pipeline = { id: string; name: string };
const categoryLabels: Record<string, string> = { training: "Treinamento", certification: "Certificação", formation: "Formação", course: "Curso", mentoring: "Mentoria", other: "Outro" };
const money = new Intl.NumberFormat("pt-BR", { style: "currency", currency: "BRL" });
const empty = { name: "", category: "course", description: "", price: "0,00", pipelineId: "", color: "#5B5BD6", active: true };
function cents(value: string) { return Math.max(0, Math.round((Number(value.replace(/[^\d,.-]/g, "").replace(/\./g, "").replace(",", ".")) || 0) * 100)); }

export default function ProductsPanel({ flash }: { flash: (message: string) => void }) {
  const [products, setProducts] = useState<Product[]>([]); const [pipelines, setPipelines] = useState<Pipeline[]>([]); const [canManage, setCanManage] = useState(false); const [form, setForm] = useState(empty); const [editing, setEditing] = useState<Product | null>(null); const [open, setOpen] = useState(false);
  const load = useCallback(async () => { const [p, f, me] = await Promise.all([fetch("/api/products"), fetch("/api/pipelines"), fetch("/api/team/me")]); setProducts(await p.json()); setPipelines((await f.json() as Pipeline[]).map(({ id, name }) => ({ id, name }))); const profile = await me.json() as { role?: string }; setCanManage(profile.role === "manager" || profile.role === "admin"); }, []);
  useEffect(() => { load(); }, [load]);
  function show(product?: Product) { setEditing(product || null); setForm(product ? { name: product.name, category: product.category, description: product.description, price: (product.priceCents / 100).toLocaleString("pt-BR", { minimumFractionDigits: 2 }), pipelineId: product.pipelineId || "", color: product.color, active: product.active } : empty); setOpen(true); }
  async function save(event: FormEvent) { event.preventDefault(); const response = await fetch(editing ? `/api/products/${editing.id}` : "/api/products", { method: editing ? "PATCH" : "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...form, priceCents: cents(form.price) }) }); if (!response.ok) return flash("Não foi possível salvar o produto."); setOpen(false); await load(); flash(editing ? "Produto atualizado." : "Produto cadastrado."); }
  async function duplicate(product: Product) { const response = await fetch(`/api/products/${product.id}/duplicate`, { method: "POST" }); if (!response.ok) return flash("Não foi possível duplicar o produto."); await load(); flash("Produto duplicado."); }
  async function remove(product: Product) { if (!confirm(`Excluir “${product.name}”?`)) return; const response = await fetch(`/api/products/${product.id}`, { method: "DELETE" }); const result = await response.json(); if (!response.ok) return flash(result.error || "Não foi possível excluir."); await load(); flash("Produto excluído."); }
  return <section className="commerce-screen"><header className="commerce-hero"><div><span className="eyebrow">PORTFÓLIO COMERCIAL</span><h2>Produtos da empresa</h2><p>Treinamentos, certificações, formações, cursos e mentorias disponíveis para venda.</p></div>{canManage && <button className="primary" onClick={() => show()}>+ Novo produto</button>}</header><div className="product-grid">{products.map((product) => <article className={product.active ? "product-card" : "product-card inactive"} key={product.id} style={{ borderTopColor: product.color }}><div className="product-card-head"><span style={{ background: product.color }}>{categoryLabels[product.category]?.slice(0, 1)}</span><div><small>{categoryLabels[product.category] || "Produto"}</small><h3>{product.name}</h3></div><i>{product.active ? "Ativo" : "Inativo"}</i></div><p>{product.description || "Sem descrição cadastrada."}</p><dl><div><dt>Valor</dt><dd>{money.format(product.priceCents / 100)}</dd></div><div><dt>Funil vinculado</dt><dd>{product.pipelineName || "Sem funil"}</dd></div></dl>{canManage && <footer><button onClick={() => show(product)}>Editar</button><button onClick={() => duplicate(product)}>Duplicar</button><button className="danger-text" onClick={() => remove(product)}>Excluir</button></footer>}</article>)}{!products.length && <div className="commerce-empty">Cadastre o primeiro produto para começar sua esteira comercial.</div>}</div>{open && <div className="modal-backdrop" onMouseDown={() => setOpen(false)}><form className="modal product-modal" onSubmit={save} onMouseDown={(event) => event.stopPropagation()}><div className="modal-head"><div><span className="eyebrow">PRODUTO</span><h2>{editing ? "Editar produto" : "Novo produto"}</h2></div><button type="button" onClick={() => setOpen(false)}>×</button></div><label>Nome<input required autoFocus value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} /></label><div className="form-grid"><label>Categoria<select className="form-select" value={form.category} onChange={(event) => setForm({ ...form, category: event.target.value })}>{Object.entries(categoryLabels).map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label><label>Valor (R$)<input inputMode="decimal" value={form.price} onFocus={(event) => event.currentTarget.select()} onChange={(event) => setForm({ ...form, price: event.target.value })} /></label></div><label>Funil de vendas<select className="form-select" value={form.pipelineId} onChange={(event) => setForm({ ...form, pipelineId: event.target.value })}><option value="">Sem funil vinculado</option>{pipelines.map((pipeline) => <option key={pipeline.id} value={pipeline.id}>{pipeline.name}</option>)}</select></label><label>Descrição<textarea rows={4} maxLength={4000} value={form.description} onChange={(event) => setForm({ ...form, description: event.target.value })} /></label><div className="product-options"><label>Cor<input type="color" value={form.color} onChange={(event) => setForm({ ...form, color: event.target.value })} /></label><label className="check-line"><input type="checkbox" checked={form.active} onChange={(event) => setForm({ ...form, active: event.target.checked })} /> Produto ativo</label></div><div className="modal-actions"><button type="button" className="secondary" onClick={() => setOpen(false)}>Cancelar</button><button className="primary">Salvar produto</button></div></form></div>}</section>;
}
